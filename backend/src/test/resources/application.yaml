spring:
  datasource:
    url: jdbc:h2:mem:sammelalbum;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  # JWT Configuration
  jwt:
    # Secret key for signing JWT tokens (Base64 encoded 256-bit key)
    secret: K7gNU3kef8297wnsJvbdw/Ba49bmGW76NFh70fE0ZeM=
    # Token expiration time in milliseconds (86400000 ms = 24 hours)
    expiration: 86400000
    # Password reset token secret (separate from login secret for security)
    password-reset-secret: R9mPX2vYq8wB5nZt7cKjH4fL6gD3sA1eU0iO9pMxN8y=
    # Password reset token expiration (7200000 ms = 2 hours)
    password-reset-expiration: 7200000
  cors:
    allowed-origins: http://localhost:42000 # 1 day
  validation-codes: "123456"
